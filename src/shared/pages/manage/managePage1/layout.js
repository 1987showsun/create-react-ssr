import { Helmet } from "react-helmet";

export default ({

}) => {
    return(
        <>
            <Helmet>
                <title>manage</title>
            </Helmet>
            <div>manage page1</div>
        </>
    );
}